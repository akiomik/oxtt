name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  lint:
    name: Format & Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install JACK development headers
        run: sudo apt-get update && sudo apt-get install -y libjack-jackd2-dev pkg-config
      - uses: Swatinem/rust-cache@v2
      - name: cargo fmt --check
        run: cargo fmt --all -- --check
      - name: cargo clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

  test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install JACK development headers
        run: sudo apt-get update && sudo apt-get install -y libjack-jackd2-dev pkg-config
      - uses: Swatinem/rust-cache@v2
      - name: cargo test
        run: cargo test --workspace --all-targets --locked
      - name: cargo build --release
        run: cargo build --workspace --release --locked
      # Named crate by crate rather than left to `--workspace` or to
      # `default-members`, because this step proves something specific: the
      # `#[no_panic]` attributes that only hold under full optimisation
      # (docs/contracts.md §6). The list is every crate that carries one.
      #
      # `oxtt-controls` is on it because `assign` runs inside Bela's audio
      # callback. `oxtt` is not: the composition of conditioning, assignment
      # and `apply_update` has no proof of its own — each half is proved in
      # the crate that owns it, which is what makes a failure attributable.
      #
      # Add a crate here when it gains a `#[no_panic]`.
      - name: cargo test --release (no-panic proofs, docs/contracts.md §6)
        run: >-
          cargo test --release --locked
          -p effectkit -p effectkit-controls -p oxtt-dsp -p oxtt-controls

  pi-controls:
    # `effectkit-controls-pi` depends on Linux-only `rppal`, so it is off the
    # macOS development machine's `default-members` and reached through the
    # workspace's one remaining feature. `rppal` builds natively here, so this
    # job type-checks, lints and tests it on the same runner as everything else
    # rather than needing a cross-compiler or a Raspberry Pi.
    name: Pi controls feature
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install JACK development headers
        run: sudo apt-get update && sudo apt-get install -y libjack-jackd2-dev pkg-config
      - uses: Swatinem/rust-cache@v2
      - name: cargo clippy --features pi-controls
        run: >-
          cargo clippy -p oxtt --features pi-controls -p effectkit-controls-pi
          --all-targets -- -D warnings
      - name: cargo test --features pi-controls
        run: >-
          cargo test -p oxtt --features pi-controls -p effectkit-controls-pi
          --all-targets --locked
      - name: cargo build --features pi-controls
        run: cargo build -p oxtt --features pi-controls --locked

  bela-device:
    # Type-checks the half of `oxtt-bela` that only exists on the board:
    # everything behind `cfg(bela_device)`, which nothing above compiles. The
    # rest of the package is an ordinary workspace member and is covered by
    # `lint` and `test` — `bela`'s device code is behind a `bela_device` cfg
    # its build script sets only for aarch64 Linux, so on an x86-64 runner the
    # crate builds with no sysroot and no cross toolchain.
    # `cargo clippy` does not link, so this needs neither `libbela` nor the
    # 800 MB sysroot a real device build copies off the board — only the
    # target's standard library. Producing an actual binary is a
    # cross-compile from a development machine (docs/bela/cross-compile.md).
    #
    # The no-panic proof cannot follow it here: proving it needs
    # `cargo test --release`, which needs to run the tests, which needs the
    # board. The proof stays a property of the source and the optimiser,
    # established on x86-64 by the `test` job's explicit crate list.
    name: Bela device target
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.91.1"
          targets: aarch64-unknown-linux-gnu
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - name: cargo clippy --target aarch64-unknown-linux-gnu
        run: cargo clippy -p oxtt-bela --all-targets --target aarch64-unknown-linux-gnu -- -D warnings

  msrv:
    name: MSRV (1.88.0)
    runs-on: ubuntu-latest
    # `dtolnay/rust-toolchain` only runs `rustup default`, which rustup ranks
    # *below* a `rust-toolchain.toml` — so the repository's 1.91.1 pin would win
    # and this job would silently re-test the same toolchain as everything else.
    # `RUSTUP_TOOLCHAIN` outranks the file, so it is what actually pins the job
    # to the MSRV. Setting it at job level also keys `rust-cache` on 1.88.0,
    # keeping this cache separate from the other jobs'.
    env:
      RUSTUP_TOOLCHAIN: "1.88.0"
    steps:
      - uses: actions/checkout@v7
      - name: Install JACK development headers
        run: sudo apt-get update && sudo apt-get install -y libjack-jackd2-dev pkg-config
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.88.0"
      - uses: Swatinem/rust-cache@v2
      # Every host is a package of its own now, so `--workspace` covers all
      # three binaries in one line. `bela`'s own MSRV is 1.85, below this one,
      # so it is expected to hold.
      - name: cargo build (MSRV)
        run: cargo build --workspace --all-targets --locked
