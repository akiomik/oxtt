[package]
name = "hyperglare-args"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true
description = "The command-line arguments hyperglare's binaries share: the chord and the processor's settings"
readme = "../../README.md"
publish = false

[dependencies]
clap = { version = "4", features = ["derive"] }
hyperglare-dsp = { path = "../hyperglare-dsp" }

[lints]
workspace = true
